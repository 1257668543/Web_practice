// ['1', '2', '3'].map(parseInt)

// ['10', '10', '10',  '10', '10'].map(parseInt)


// parseInt(100)  // 10进制

// parseInt(string, [radix])

parseInt('a') // 10进制

// 以第二个参数radix为基数解析第一个参数，转化成十进制输出
parseInt(100, 0)
// '0x', '0X'  --- 16


// console.log(
//   [1, 2, 3].map((e) => {
//     return e
//   })
// );


// var new_array = arr.map(function callback(currentVal[,index[,array]]) { // 第三个参数是callback map方法调用的数组

// },[, thisArg])

const arr = [1, 2, 3]
arr.map(() => {
  num + 1
})